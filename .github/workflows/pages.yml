name: Hexo Pages

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Cache npm
        uses: actions/cache@v4
        with:
          path: myblogs/node_modules
          key: ${{ runner.os }}-npm-${{ hashFiles('myblogs/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-npm-

      - name: Install dependencies
        working-directory: ./myblogs
        run: npm install

      - name: Clean Hexo
        working-directory: ./myblogs
        run: npx hexo clean

      - name: Generate site
        working-directory: ./myblogs
        run: npx hexo generate

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./myblogs/public

      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4
